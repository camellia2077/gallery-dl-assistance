## v0.1.0.1 - 2025-12-13

* **支持下载live photo**

* **支持用toml作为配置文件**

* **运行前先检测gallery-dl是否下载**

* **加入time.sleep()防止触发反爬虫冷却**

## v0.1.0.2 - 2025-12-13

* **下载后统计mp4(实况图片)的下载个数**

* **修复url错误获取，为null的问题**


* **命令行新增是否重试下载的选项**

* **命令行传入空的指令，会从toml中读取**
优先级 命令行＞toml

* **强制更新元数据**
既然程序每次运行都会调用 API 获取最新的数据（这一步最耗时），那么把这份最新的、修复好的数据覆盖写入到硬盘（这一步极快）是完全合理的，不应该跳过。

## v0.1.0.2 - 2025-12-14

* **生成的log文件自动按照月份分类**